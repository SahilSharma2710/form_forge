import 'package:flutter_test/flutter_test.dart';

void main() {
  group('form_forge package', () {
    test('package is importable', () {
      // Verifies the package resolves and the barrel export exists.
      // Actual annotation tests will be added in Story 1.2+.
      expect(true, isTrue);
    });
  });
}
